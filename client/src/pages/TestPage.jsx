import React from 'react';

// --- ¡ARREGLO! ---
// Añadimos "export default" para que App.jsx pueda importarlo
export default function TestPage() {
  return (
    <div style={{ padding: 'clamp(12px, 4vw, 20px)', maxWidth: '100%', boxSizing: 'border-box' }}>
      <h2>Test Page</h2>
      <p>Esta sección está en construcción.</p>
    </div>
  );
}

